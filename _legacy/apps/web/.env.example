DATABASE_URL="file:./dev.db"
# Required: long random string used to sign session cookies (middleware + server must match).
SESSION_SECRET="change-me-in-production-use-long-random-string"
# Optional: protect POST /api/internal/jobs/drain (rollup / rolling-restart worker).
INTERNAL_JOB_SECRET=""
